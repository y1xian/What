ext.isLibrary = true
apply from: rootProject.file('lib.gradle')

dependencies {
    api project(':widget:lib_common')
    //  v7 v4 约束
    api([libs.fragment, libs.constraint])
    //bus
    api libs.live_event_bus

    implementation libs.startup
}
