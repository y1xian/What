ext.isLibrary = true
apply from: rootProject.file('lib.gradle')

dependencies {
    compileOnly libs.widget_common
    // jetpack
    compileOnly libs.arch_comp_extensions
    // rx全家桶
    api([libs.rxjava2, libs.rxandroid2, libs.rxbinding2])
}
