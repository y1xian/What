ext.isLibrary = true
apply from: rootProject.file('lib.gradle')

dependencies {
    api project(':lib_common')
    //  v7 v4 约束
    api([libs.fragment, libs.annotations, libs.constraint])
    //bus
    api libs.live_event_bus
}
