ext.isPublish = true
apply from: rootProject.file('lib-util.gradle')

dependencies {
    // jetpack
    api libs.lifecycle_java8, libs.lifecycle
    implementation libs.lib_app
    api libs.hutool_core
}