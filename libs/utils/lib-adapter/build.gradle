ext.isPublish = true
apply from: rootProject.file('lib-util.gradle')

android {
    buildFeatures {
        dataBinding true
    }
}

dependencies {
    implementation libs.lib_core
    api libs.recyclerview
}
