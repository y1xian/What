ext.isPublish = true
apply from: rootProject.file('lib_util.gradle')

dependencies {
    implementation(project(':utils:lib-app'), project(':utils:lib-core'))
    // okhttp3
    api libs.okhttp3, libs.okhttp3_logging
    // gson  https://github.com/google/gson
    api libs.gson
}