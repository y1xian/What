ext.isPublish = true
apply from: rootProject.file('lib_util.gradle')

dependencies {
    implementation(libs.lifecycle_extensions)
    implementation(project(':utils:util-app'), project(':utils:util-common'))
}