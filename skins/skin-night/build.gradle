ext.mainApp = true
apply from: rootProject.file('simple.gradle')

android {
    defaultConfig {
//        applicationId "com.yyxnb.skin_night"
    }

    android.applicationVariants.all { variant ->
        variant.outputs.all {
            // 把生成的apk放到 module-user 模块，assets/skins/night.apk
            outputFileName = "night.apk"
        }
    }
}
