ext.isPublish = true
apply from: rootProject.file('lib_widget.gradle')

dependencies {
    api libs.glide
    annotationProcessor libs.glide_compiler
    implementation project(':widgets:lib_image_loader')
    implementation libs.recyclerview
}
