ext.isPublish = true
apply from: rootProject.file('lib_widget.gradle')

dependencies {
    // 通用
    api project(':widgets:lib_common')
    // 底层架构
    api project(':widgets:lib_arch')
    // 网络框架
    api project(':widgets:lib_network')
    // 数据库
    api project(':widgets:lib_room')
    // 图片框架
    api project(':widgets:lib_image_loader')
    // 本地服务
    api project(':widgets:lib_localservice')
    // 对话框
    api project(':widgets:lib_dialog')
    api project(':widgets:lib_popup')
    // 适配器
    api project(':widgets:lib_adapter')
    // 自定义view
    api project(':widgets:lib_view')
    // 悬浮窗
    api project(':widgets:lib_floatwindow')
    // 换肤
    api project(":widgets:lib_skinloader")
    // 文件处理 下载、上传
    api project(":widgets:lib_file")
    // 音乐
    api project(":widgets:lib_music")
    // 视频
    api project(":widgets:lib_video")
    // web
    api project(":widgets:lib_webview")
}
