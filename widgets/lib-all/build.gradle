ext.isPublish = true
apply from: rootProject.file('lib_widget.gradle')

dependencies {
    // 通用
    api project(':widgets:lib-common')
    // 底层架构
    api project(':widgets:lib-arch')
    // 网络框架
    api project(':widgets:lib-network')
    // 数据库
    api project(':widgets:lib-room')
    // 图片框架
    api project(':widgets:lib-image')
    // 本地服务
    api project(':widgets:lib-localservice')
    // 对话框
    api project(':widgets:lib-dialog')
    api project(':widgets:lib-popup')
    // 适配器
    api project(':widgets:lib-adapter')
    // 自定义view
    api project(':widgets:lib-view')
    // 悬浮窗
    api project(':widgets:lib-floatwindow')
    // 换肤
    api project(":widgets:lib-skinloader")
    // 文件处理 下载、上传
    api project(":widgets:lib-file")
    // 音乐
    api project(":widgets:lib-music")
    // 视频
    api project(":widgets:lib-video")
    // web
    api project(":widgets:lib-webview")
}
